[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "utility-asset-registry"
version = "0.1.0"
description = "Clean field survey CSVs and serve utility asset records over HTTP."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "sqlalchemy>=2.0.35",
    "pydantic>=2.9.0",
    "pydantic-settings>=2.5.0",
    "python-jose[cryptography]>=3.3.0",
    "pwdlib[bcrypt]>=0.2.0",
    "python-multipart>=0.0.12",
    "typer>=0.12.0",
]

[project.scripts]
asset-ingest = "utility_asset_registry.cli:run"
asset = "utility_asset_registry.cli:operator_main"
load = "utility_asset_registry.cli:load_main"
start = "utility_asset_registry.cli:start_main"
run = "utility_asset_registry.cli:menu_main"
setup = "utility_asset_registry.cli:setup_main"
check = "utility_asset_registry.cli:check_main"
rejects = "utility_asset_registry.cli:rejects_main"

[project.optional-dependencies]
dev = [
    "pytest>=8.3.0",
    "httpx>=0.27.0",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "tests"]
